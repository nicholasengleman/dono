# import * from './generated/prisma.graphql'

type Mutation {
  createProduct(
    name: String
    description: String
    image: String
    largeImage: String
    price: Int
  ): Product!
}

type Query {
  products: [Product]!
}
